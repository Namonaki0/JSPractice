const path = location.pathname;

console.log(path);

//* "/cp/internal/boomboom/index.html";

// -----------------------------------------------

//? The pathname property sets or returns the pathname of a URL (page).
