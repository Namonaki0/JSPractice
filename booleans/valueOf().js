let boolean = false;
const value = boolean.valueOf();

console.log(value);

//* false

// -----------------------------------------------

//? valueOf() returns the primitive value of a boolean.
//? valueOf() is usually called by JavaScript behind the scenes, and not explicitly in code.
