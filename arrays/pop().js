const names = ["John", "Grace", "Phil", "James"];

const lastEl = names.pop();

console.log(lastEl);

//* "James"

const names = ["John", "Grace", "Phil", "James"];

names.pop();

console.log(names);

//* ["John", "Grace", "Phil"];

// -----------------------------------------------

//? The pop() method removes (pops) the last element of an array.
//? The pop() method changes the original array.
//? The pop() method returns the removed element.
